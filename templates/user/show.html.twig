{% extends 'base.html.twig' %}


{% block body_id 'nos_origines_show' %}
{% block Carousel %}
{% endblock %}

{% block main %}
 
<ul class="nav nav-tabs" id="myTab" role="tablist">
 <li class="nav-item">
    <a class="nav-link active" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true">Coordonnées</a>
 </li>
    {#
 <li class="nav-item">
  <a class="nav-link" id="profile-tab" data-toggle="tab" href="#profile" role="tab" aria-controls="profile" aria-selected="false">Photo</a>
 </li>
 <li class="nav-item">
  <a class="nav-link" id="contact-tab" data-toggle="tab" href="#contact" role="tab" aria-controls="contact" aria-selected="false">Infos additionnelles</a>
 </li>
    #}
</ul>


<div class="tab-content" id="myTabContent">
  <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">
       <p><h3>Evenement</h3></p>
	{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
  <div class="row">
     <div class="col">
			<p class="font-weight-bold pt-2 rounded w-100">{{ form_label(form.eventDate) }}</p>
        </div>
        <div class="col" id="some-custom-id"> {{ form_widget(form.eventDate) }}</div>
		 <div class="col">
		 <p class="font-weight-bold pt-2 rounded w-100">{{ form_label(form.eventType) }} </p>
        </div>
        <div class="col" id="some-custom-id">
          {{ form_widget(form.eventType) }}
        </div>
  </div>


	<div class="row">
        <div class="col">
            <p class="font-weight-bold pt-2 rounded w-100">{{ form_label(form.eventLieu) }} </p>
        </div>
        <div class="col" id="some-custom-id">
            {{ form_widget(form.eventLieu) }}
        </div>

	 <div class="col">
		 <p class="font-weight-bold pt-2 rounded w-100">{{ form_label(form.eventInfos) }} </p>
        </div>
        <div class="col" id="some-custom-id">
          {{ form_widget(form.eventInfos) }}
        </div>
		
    </div>


  
  {# ######################################################## #}

</div>
</div>
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 		
{#
       <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> Save changes
        </button>
#}
		{{ form_end(form) }}
 
{#
	 <div class="section actions">
        {{ include('admin/membre/_delete_form.html.twig', {personne: personne}, with_context = false) }}
    </div>
#}
    </p>	
  
{% endblock %}

{% block sidebar %}
<p>
   <div class="section actions">
   {#
        <a href="{{ path('admin_membre_new') }}" class="btn btn-lg btn-block btn-success">
            <i class="fa fa-plus" aria-hidden="true"></i> Nouveau membre
        </a>
   #}
   </div>
</p>
 {% endblock %}