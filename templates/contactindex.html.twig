{% extends 'base.html.twig' %}


{% block body_id 'nos_origines_show' %}
{% block Carousel %}
{% endblock %}

{% block main %}
 
<ul class="nav nav-tabs" id="myTab" role="tablist">
 <li class="nav-item">
    <a class="nav-link active" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true">Coordonnées</a>
 </li>
 <li class="nav-item">
  <a class="nav-link" id="profile-tab" data-toggle="tab" href="#profile" role="tab" aria-controls="profile" aria-selected="false">Photo</a>
 </li>
 <li class="nav-item">
  <a class="nav-link" id="contact-tab" data-toggle="tab" href="#contact" role="tab" aria-controls="contact" aria-selected="false">Infos additionnelles</a>
 </li>
</ul>


<div class="tab-content" id="myTabContent">
  <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">
      
	{{ form_start(form) }}
  <div class="row">
		 <div class="col">
		    <p class="font-weight-bold pt-2 rounded w-100">{{ form_row(form.firstname) }} </p>
        </div>
        <div class="col">
             <p class="font-weight-bold pt-2 rounded w-100">{{ form_row(form.lastname) }} </p>
        </div>
    </div>
	
	
      <div class="row">
		 <div class="col">
		    <p class="font-weight-bold pt-2 rounded w-100">{{ form_row(form.phone) }} </p>
        </div>
         <div class="col">
            <p class="font-weight-bold pt-2 rounded w-100">{{ form_row(form.email) }} </p>
        </div>
     </div>
 
    
  {# ######################################################## #}
  {{ form_rest(form) }}
</div>
<button type="submit" class="btn btn-primary">
<i class="fa fa-save" aria-hidden="true"></i> Envoyer
</button>

		{{ form_end(form) }}
 
  
{% endblock %}

{% block sidebar %}
<p>
   <div class="section actions">
   {#
        <a href="{{ path('admin_membre_new') }}" class="btn btn-lg btn-block btn-success">
            <i class="fa fa-plus" aria-hidden="true"></i> Nouveau membre
        </a>
   #}
   </div>
</p>
{{parent()}}
 {% endblock %}