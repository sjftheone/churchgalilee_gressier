{% extends 'base.html.twig' %}

{% block body_id 'nos_origines_show' %}
{% block Carousel %}
{% endblock %}

{% block main %}
<p>
 <h3>Utilisateur Création </h3>
</p>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="row">
        <div class="col">
            <p class="font-weight-bold pt-2 rounded w-100">{{ form_label(form.username) }}{{ form_widget(form.username) }}</p>
        </div>
       
        <div class="col">
            <p class="font-weight-bold pt-2 rounded w-100">{{ form_label(form.fullName) }}{{ form_widget(form.fullName) }} </p>
        </div>
        
        <div class="col">
            <p class="font-weight-bold pt-2 rounded w-100">{{ form_label(form.email) }} {{ form_widget(form.email) }} </p>
        </div>
    </div>


     <div class="row">
        <div class="col">
        {#    <p class="font-weight-bold pt-2 rounded w-100">{{ form_label(form.Roles) }}<br>{{ form_widget(form.Roles) }} </p>#}
           <p class="font-weight-bold pt-2 rounded w-100">{{ form_label(form.Roles)}}{{form_widget(form.Roles)}}</p>

        </div>
        


    <div class="col">
            <p class="font-weight-bold pt-2 rounded w-100">{{ form_widget(form.plainPassword) }}</p>
        </div>
     <div class="col">
             <button type="submit" class="btn btn-primary">
        <i class="fa fa-save" aria-hidden="true"></i> Save changes
    </button>
        </div>
       
       
    </div>
{#
    <div class="row">

        <div class="col">
            <p class="font-weight-bold pt-2 rounded w-100">{{ form_label(form.eventLieu) }} </p>
        </div>
        <div class="col" id="some-custom-id">
            {{ form_widget(form.eventLieu) }}
        </div>

        <div class="col">
            <p class="font-weight-bold pt-2 rounded w-100"> {{ form_label(form.eventInfos) }}</p>
        </div>
        <div class="col" id="some-custom-id">
            {{ form_widget(form.eventInfos) }}
        </div>


    </div>
    #}

    {{ form_rest(form) }}
   

{{ form_end(form) }}


    <div></div>
 
{% endblock %}

{% block sidebar %}
 {{ parent() }}

{#
<p>
   <div class="section actions">
        <a href="{{ path('admin_membre_new') }}" class="btn btn-lg btn-block btn-success">
            <i class="fa fa-plus" aria-hidden="true"></i> Nouveau membre
        </a>
    </div>
</p>
#}
 {% endblock %}
