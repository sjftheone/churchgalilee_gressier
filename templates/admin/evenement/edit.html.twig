{% extends 'base.html.twig' %}

{% block body_id 'nos_origines_show' %}
{% block Carousel %}
{% endblock %}

{% block main %}
{% for message in app.flashes('successs')%}
  <div class="alert alert-success">
   {{message}}
  </div>
 {% endfor %}
<p>
 <h4>Modifier événement </h4>
</p>

{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="row">
        <div class="col">
			<p class="font-weight-bold pt-2 rounded w-100">{{ form_label(form.eventDate) }}</p>
        </div>
        <div class="col" id="some-custom-id"> {{ form_widget(form.eventDate) }}</div>
		
		 <div class="col">
		 <p class="font-weight-bold pt-2 rounded w-100">{{ form_label(form.eventType) }} </p>
        </div>
        <div class="col" id="some-custom-id">
          {{ form_widget(form.eventType) }}
        </div>
    </div>
	
	<div class="row">
	 <div class="col">
		 <p class="font-weight-bold pt-2 rounded w-100">{{ form_label(form.eventLieu) }} </p>
        </div>
        <div class="col" id="some-custom-id">
          {{ form_widget(form.eventLieu) }}
        </div>
		
        <div class="col">
		<p class="font-weight-bold pt-2 rounded w-100"> {{ form_label(form.eventInfos) }}</p>
        </div>
        <div class="col" id="some-custom-id">
            {{ form_widget(form.eventInfos) }}
        </div>
    </div>

        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> Save changes
        </button>

		{{ form_end(form) }}
		
	<div class="section actions">
        {{ include('admin/evenement/_delete_form.html.twig', {evenement: evenement}, with_context = false) }}
    </div>

{#
	   <div class="form-group row">
                  <label for="emailpetit" class="col-auto col-form-label col-form-label-sm">{{form_label(form.nom)}}</label>
                  <div class="col">
                   <label for="emailpetit" class="col-auto col-form-label col-form-label-sm">{{form_widget(form.nom)}}</label>
                  </div>
        </div>
</p>


   
	   {{form_rest(form)}}
			<button type="submit" class="btn btn-primary">
            <i class="fa fa-edit" aria-hidden="true"></i> edit
        </button>
              
		
        {{ form_end(form) }}
    </p>	
   
 
 <table class="table table-striped table-middle-aligned">
        <thead>
            <tr>
                <th scope="col">Nom et Prenom</th>
                <th scope="col"><i class="fa fa-calendar" aria-hidden="true"></i> Date de Naissance</th>
				<th scope="col">Phone</th>
                <th scope="col" class="text-center"><i class="fa fa-cogs" aria-hidden="true"></i> Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for personne in personnes %}
            <tr>
                <td>{{ personne.civilite }} {{personne.nom}} {{personne.prenom}}</td>
                <td>{{ personne.date_naissance}}</td>
				 <td>{{ personne.phone_personnel}}</td>
                <td class="text-right">
                    <div class="item-actions">
                        <a href="{{ path('admin_membre_show', {id: membre.id}) }}" class="btn btn-sm btn-default">
                            <i class="fa fa-eye" aria-hidden="true"></i> Show
                        </a>

                        <a href="{{ path('admin_membre_edit', {id: membre.id}) }}" class="btn btn-sm btn-primary">
                            <i class="fa fa-edit" aria-hidden="true"></i> edit
                        </a>
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4" align="center">Aucun Membre found</td>
           </tr>
        {% endfor %}
        </tbody>
    </table>
 #}
 <div></div>
{% endblock %}

{% block sidebar %}
{#
<p>
   <div class="section actions">
        <a href="{{ path('admin_membre_new') }}" class="btn btn-lg btn-block btn-success">
            <i class="fa fa-plus" aria-hidden="true"></i> Nouveau membre
        </a>
    </div>
</p>
#}
 {% endblock %}
