{% extends 'base.html.twig' %}
{% block body_id 'nos_origines_show' %}
{% block Carousel %}
{% endblock %}

{% block main %}
    <h1>Creation d'un Post</h1>

    {{ form_start(form) }}
<div class="row">
            <div class="col">
                 <div class="alert alert-secondary" > {{form_errors(form.title)}} {{form_errors(form.summary)}} {{form_errors(form.content)}} {{form_errors(form.imageFile)}}</div>
            </div>
        </div>

<table class="table table-striped table-middle-aligned table-sm">
       
        <tbody>        
            <tr>			 
                <td>{{ form_label(form.title) }}</td>
                <td>{{ form_widget(form.title)}}</td>            
	        </tr> 
            <tr>			 
                <td>{{ form_label(form.summary) }}</td>
                <td>{{ form_widget(form.summary)}}</td>            
	        </tr>

            <tr>			 
                <td>{{ form_label(form.content) }}</td>
                <td>{{ form_widget(form.content)}}</td>            
	        </tr>

             <tr>			 
                <td>{{ form_label(form.imageFile) }}</td>
                <td>{{ form_widget(form.imageFile)}}</td>            
	        </tr>

             <tr>			 
                <td>{{ form_label(form.publishedAt) }}</td>
                <td>{{ form_widget(form.publishedAt)}}</td>            
	        </tr>

	</table>

    <button type="submit" class="btn btn-primary">
        <i class="fa fa-save" aria-hidden="true"></i> {{ 'create post'|trans }}
    </button>
    {{ form_widget(form.saveAndCreateNew, {label: 'save and create new', attr: {class: 'btn btn-primary'}}) }}
    <a href="{{ path('admin_post_index') }}" class="btn btn-link">
        <i class="fa fa-list-alt" aria-hidden="true"></i> back to list
    </a>
    {{ form_end(form) }}
{% endblock %}

{% block sidebar %}
    {#
    <p>
       <div class="section actions">
            <a href="{{ path('admin_membre_new') }}" class="btn btn-lg btn-block btn-success">
                <i class="fa fa-plus" aria-hidden="true"></i> Nouveau membre
            </a>
        </div>
    </p>
    #}
{% endblock %}
