{% extends 'base.html.twig' %}

{% block body_id 'user_password' %}
{% block Carousel %}
{% endblock %}
{% block main %}
    <h3>change password</h3>

    <div class="alert alert-info" role="alert">After changing your password, you will be logged out of the application.</div>


 <div class="row">
 <div class="col">
 {{ form_start(form) }}
     <table class="table table-striped table-middle-aligned table-sm">       
        <tbody>        
            <tr>			 
                <td>{{ form_label(form.currentPassword) }}</td>
            </tr>
            <tr>
                <td>{{ form_widget(form.currentPassword)}}</td>
            
            <tr>
                <td>{{ form_widget(form.newPassword)}}</td>                
            </tr>
           
        </tbody>
    </table>
 
        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> Save
        </button>
    {{ form_end(form) }}

 </div>

 <div class="col">
 </div>
 </div>


{% endblock %}

{% block sidebar %}
    <div class="section">
        <a href="{{ path('user_personnel_edit')}}" class="btn btn-lg btn-block btn-danger">
            <i class="fa fa-edit" aria-hidden="true"></i> Edit User
        </a>
    </div>

    {{ parent() }}


{% endblock %}
